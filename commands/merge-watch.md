---
description: "Continuously poll and fix a PR until all readiness gates pass, then auto-merge"
argument-hint: "[PR#]"
---

Read the skill file at ${CLAUDE_PLUGIN_ROOT}/skills/merge-watch/SKILL.md and follow it exactly. The user's message after /merge-watch is the optional PR number argument.


ARGUMENTS: $ARGUMENTS
