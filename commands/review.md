---
description: "Multi-perspective code review (code + PM + design + input edge-cases) with auto-fix. Conditionally skips PM/Design agents when upstream gates passed."
argument-hint: "[PR#]"
---

Read the skill file at ${CLAUDE_PLUGIN_ROOT}/skills/review/SKILL.md and follow it exactly. The user's message after /review is the optional PR number argument.


ARGUMENTS: $ARGUMENTS
