---
description: "Development lifecycle orchestrator: intake → workspace → brainstorm → plan → implement → review → PR → merge → retro, with size-based ceremony"
argument-hint: "[ticket-id or description]"
---

Read the skill file at ${CLAUDE_PLUGIN_ROOT}/skills/dev/SKILL.md and follow it exactly. The user's message after /dev is the task context argument.


ARGUMENTS: $ARGUMENTS
