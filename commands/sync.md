---
description: "Pull or push knowledge base changes. Subcommands: pull, push, status (default)."
argument-hint: "[pull|push|status]"
---

Read the skill file at ${CLAUDE_PLUGIN_ROOT}/skills/sync/SKILL.md and follow it exactly. The user's message after /pm:sync is the subcommand (pull, push, or status).
