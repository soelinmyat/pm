---
description: "Manually pull or push knowledge base changes. Usage: /pm:sync push, /pm:sync pull, /pm:sync status, /pm:sync setup"
argument-hint: "<setup|push|pull|status>"
---

Read the skill file at ${CLAUDE_PLUGIN_ROOT}/skills/sync/SKILL.md and follow it exactly. The user's message after /pm:sync contains the subcommand (setup, push, pull, or status).
