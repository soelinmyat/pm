---
description: "Configure PM integrations, repository paths, or execution preferences. Use Start to initialize a product workspace."
argument-hint: "<enable|disable> <integration>"
---

Read the skill file at ${CLAUDE_PLUGIN_ROOT}/skills/setup/SKILL.md and follow it exactly. The user's message after /pm:setup contains the action (enable/disable) and integration name.
