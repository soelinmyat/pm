---
description: "Import customer evidence from a file or folder path, normalize it into .pm/, and update shared research artifacts in pm/research/."
argument-hint: "<path>"
---

Invoke the pm:ingest skill and follow it exactly as presented to you. The user's message after /pm:ingest is the file or folder path to ingest. If no path is provided, follow the skill's no-argument flow.
