---
description: "Loop orchestration — show the git-backed board, dry-run wake selection, inspect config, or prepare scheduler setup."
argument-hint: "<status|wake|config|install> [--dry-run] [--mode dev|ship|research]"
---

Read the skill file at ${CLAUDE_PLUGIN_ROOT}/skills/loop/SKILL.md and follow it exactly. The user's message after /pm:loop contains the subcommand and options.
