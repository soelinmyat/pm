---
description: "Merge a PR to main, delete remote branch, and clean up local branch + worktree"
argument-hint: "[PR#]"
---

Read the skill file at ${CLAUDE_PLUGIN_ROOT}/skills/merge-watch/SKILL.md and follow the "# /merge" section (located after the merge-watch section). The user's message after /merge is the optional PR number argument.


ARGUMENTS: $ARGUMENTS
