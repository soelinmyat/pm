---
name: simplify
description: "Deprecated — absorbed into pm:review in v1.9. Invoking this runs evidence-bound Review, whose reuse/quality/efficiency logical lenses replace the old simplify gate."
---

# pm:simplify (deprecated)

`pm:simplify` was absorbed into `pm:review` in v1.9. Read `${CLAUDE_PLUGIN_ROOT}/skills/review/SKILL.md` and follow it exactly — its six logical lenses include reuse, quality, and efficiency while adapting physical reviewer count. Write the `review` gate row, not a `simplify` row.
